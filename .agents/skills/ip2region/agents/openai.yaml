interface:
  display_name: "Ip2region"
  short_description: "Repo-specific patterns and workflows for ip2region"
  default_prompt: "Use the ip2region repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true